# Signing material — NEVER commit. Back these up securely; without them you
# cannot publish updates to the Play Store.
/android-upload.keystore
/keystore.env

# scala-cli build output for the tooling project
/tooling/.scala-build/
/tooling/.bsp/

# Bubblewrap-generated Android project (regenerated by `bubblewrap update`)
/app/
/gradle/
/gradlew
/gradlew.bat
/build.gradle
/settings.gradle
/gradle.properties
/local.properties
/.gradle/
/build/
/store_icon.png
/manifest-checksum.txt
/*.apk
/*.aab
/*.idsig
/twa-manifest.json.bak
